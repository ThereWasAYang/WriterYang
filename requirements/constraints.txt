# WriterYang 已验证的直接依赖工具链（Python 3.11-3.13）。
# 应用依赖仍由 pyproject.toml 声明；CI/本地门禁用 -c 约束到同一组版本。
# Dependabot 负责提出可审计的升级 PR，升级后必须跑完整质量门禁。
build==1.5.0
mypy==2.1.0
pip-audit==2.10.1
playwright==1.60.0
pydantic==2.13.4
pytest==9.0.3
pytest-cov==7.1.0
python-docx==1.2.0
PyYAML==6.0.3
ruff==0.15.14
twine==6.2.0
